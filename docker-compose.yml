services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - rembg-models:/root/.u2net
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  rembg-models:
